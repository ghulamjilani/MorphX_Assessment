<p>
  Please review the following changes for <%= link_to @channel.always_present_title, @channel.absolute_path %>
</p>

<ul>
  <% @changed_attrs.each do |key, values| %>
    <li>
      <%= channel_key_title(key) %> from &quot;<%= channel_value_title(key, values.first) %>&quot; to &quot;<%= channel_value_title(key, values.last) %>&quot;
    </li>
  <% end %>
</ul>

<p class="thank_you_message">
  <%= simple_format I18n.t('mailer.thank_you_message', service_name: Rails.application.credentials.global[:service_name]) %>
</p>
