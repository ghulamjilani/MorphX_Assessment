# frozen_string_literal: true

require 'spec_helper'

describe RoomsController do
  #   describe "POST :toggle_notify_me" do
  #     let!(:session) { create(:published_session) }
  #     let(:current_user) { create(:user) }

  #     before do
  #       sign_in(current_user, scope: :user)
  #     end

  #     it 'works' do
  #       post :toggle_notify_me, xhr: true, params: { id: session.room.id}
  #       expect(response).to be_successful
  #       expect(PendingVodAvailabilityMembership.count).to eq(1)

  #       post :toggle_notify_me, xhr: true, params: { id: session.room.id}
  #       expect(response).to be_successful
  #       expect(PendingVodAvailabilityMembership.count).to eq(0)
  #     end
  #   end
end
